# Recall Website

This is the Recall documentation site.

It uses Next.js, React, Fumadocs UI, and static export for GitHub Pages.

Run the development server:

```bash
pnpm dev
```

Build the static site:

```bash
pnpm build
```

The GitHub Pages deployment publishes `website/out` at `https://samzong.github.io/Recall/`.
